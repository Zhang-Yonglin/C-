1.  完成讲义中的自定义的数组类，并且添加常用的操作符重载

2.  完成自定义的string类

3.  操作符重载<<操作符， 能够写成一个自定义类的成员函数？

4.  对于|| 和 && 符号的操作符重载，会有什么问题？  不会发生短路

5. C++中 string类 是不是普通数据类型？是一个复杂的类，不是一个普通数据类型

6 = 操作符重载需要注意的地方：1判断是否自身赋值  2回收垃圾  3深度拷贝  4返回自身

